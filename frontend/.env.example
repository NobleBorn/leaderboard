# URL of the deployed backend API (no trailing slash), e.g.:
# VITE_API_URL=https://leaderboard-api.onrender.com
#
# Leave unset for local dev — Vite's proxy (see vite.config.js) forwards
# /api requests to http://localhost:3001 automatically.
VITE_API_URL=

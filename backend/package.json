{
  "name": "leaderboard-backend",
  "version": "1.0.0",
  "description": "Summer tournament leaderboard backend",
  "main": "src/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings src/server.js",
    "dev": "nodemon --exec 'node --no-warnings' src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
